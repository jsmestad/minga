## Summary

- Add session-scoped MCP stdio support for one configured server, including initialize, initialized notification, tools/list, and tools/call.
- Expose MCP tools through the native provider alongside built-in tools with prefixed names and JSON schemas.
- Remove MCP tools and emit a system error when the MCP client exits while keeping built-in tools available.
- Document MCP configuration with a worked filesystem-server example.

Closes #1406.

## Testing

- `make lint`
- `mix test.llm`
- `mix test.llm test/minga_agent/mcp test/minga_agent/providers/native_mcp_test.exs test/minga_agent/session_test.exs`

## Review

- Reviewer gate: PASS
