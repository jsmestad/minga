## Summary

- Add output style discovery from global and project directories, with project styles overriding global styles by filename stem.
- Add `/style`, `/style <name>`, `/style none`, and `/style off` to list, select, and clear session-scoped output styles.
- Prepend the selected style body to the native provider system prompt for future turns, and reset to `none` on new sessions.
- Surface the current style in the TUI modeline and macOS GUI status bar.
- Document style locations and a worked `review.md` example.

Part of #1404.
Closes #1412.

## Testing

- `mix test.llm test/minga_agent/output_styles_test.exs test/minga_agent/session_test.exs test/minga_editor/agent/slash_command_test.exs test/minga_editor/shell/traditional/modeline_test.exs test/minga_editor/integration/gui_protocol_test.exs`
- `mix test.debug test/minga_agent/providers/native_test.exs`
- `mix swift.build`
- `xcodebuild test -project macos/Minga.xcodeproj -scheme Minga -destination 'platform=macOS' -skipPackagePluginValidation`
- `make lint`
- `mix test.llm`

## Review

- Reviewer gate: PASS
